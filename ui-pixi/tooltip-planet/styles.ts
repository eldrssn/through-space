import * as PIXI from 'pixi.js'

export const nameStyles = new PIXI.TextStyle({
  fontSize: 40,
  fill: '#FFFFFF',
  wordWrap: true,
})

export const closeTextStyle = new PIXI.TextStyle({
  fontSize: 32,
  fill: '#ffffff',
  // fontWeight: 'bold',
})
